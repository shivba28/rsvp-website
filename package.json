{
  "name": "rsvp-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/generate-images.js && next dev",
    "build": "node scripts/generate-images.js && next build",
    "start": "next start",
    "export": "node scripts/generate-images.js && next build",
    "deploy": "npm run build && touch out/.nojekyll && gh-pages -d out"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "framer-motion": "^12.23.12",
    "next": "15.4.5",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "eslint": "^9",
    "eslint-config-next": "15.4.5",
    "tailwindcss": "^4"
  }
}
